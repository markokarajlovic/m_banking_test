part of 'ips.dart';

class IpsScreen extends StatelessWidget {
  const IpsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('IPS Screen'),
    );
  }
}
